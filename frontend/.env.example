# App title (shown in browser tab)
VITE_APP_TITLE=Tryon Collector

# API base URL — empty string = use Vite proxy (recommended for dev)
VITE_API_BASE_URL=

# API mode: 'live' (default) or 'e2e' (mock backend, no disk writes)
VITE_API_MODE=live
